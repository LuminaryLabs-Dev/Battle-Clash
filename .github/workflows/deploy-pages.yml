name: Deploy Battle Clash to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run deterministic simulation checks
        run: npm run check

      - name: Validate semantic domain coverage
        run: npm run check:domains

      - name: Validate release manifest
        run: npm run check:release

      - name: Build production site
        run: npm run build

      - name: Build Pages artifact
        run: npm run build:pages

      - name: Verify Pages artifact
        run: test -f docs/index.html

      - name: Smoke-test Pages artifact
        run: PAGES_ARTIFACT_ROOT=docs npm run check:pages-artifact

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: docs

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
